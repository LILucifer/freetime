spring:
  application:
    name: service-consumer
  cloud:
      nacos:
        server-addr: 127.0.0.1:8848
        config:
          server-addr: ${spring.cloud.nacos.server-addr}
          file-extension: yaml